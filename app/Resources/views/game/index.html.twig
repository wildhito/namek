{% extends 'base.html.twig' %}

{% block body %}
    <h1>Games list</h1>

    <table>
        <thead>
            <tr>
                <th>Shortname</th>
                <th>Fullname</th>
                <th>Brief</th>
                <th>Review</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for game in games %}
            <tr>
                <td>{{ game.shortname }}</td>
                <td>{{ game.fullname }}</td>
                <td>{{ game.brief }}</td>
                <td>{{ game.review }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('game_show', { 'shortname': game.shortname }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('game_edit', { 'id': game.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('game_new') }}">Create a new game</a>
        </li>
    </ul>
{% endblock %}
