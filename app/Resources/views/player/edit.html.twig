{% extends 'base.html.twig' %}

{% block content %}
<div class="section no-pad-bot">
  <div class="container">
    <h1 class="header center orange-text">{{ player.login }}</h1>
  </div>
</div>

<div class="section">
  <div class="container">
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn btn-large" type="submit" value="{{ 'Update'|trans }}" />
    {{ form_end(edit_form) }}
  </div>
</div>
{% endblock %}
